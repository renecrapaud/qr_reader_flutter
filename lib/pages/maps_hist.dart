import 'package:flutter/material.dart';

class MapsHist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('historial de mapas'),
    );
  }
}
